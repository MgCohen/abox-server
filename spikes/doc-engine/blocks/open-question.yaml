type: open-question
collection: true
group: Open Questions
description: A genuinely unresolved decision the reader must make.
rubric:
  - State the question and the realistic options.
  - Put the recommended default in `lean`.
  - If already decided, it is a decision, not an open question.
  - The group goes last.
attrs:
  lean: string
body: markdown
