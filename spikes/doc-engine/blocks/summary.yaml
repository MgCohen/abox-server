type: summary
short: One-paragraph what-and-why at the very top.
usage: |
  Outcome-first prose. State the objective and what "done" means in concrete
  terms. A reader with no prior context should grasp the goal and the stakes.
  Real sentences, not a bullet skeleton — but one tight paragraph, not a wall.
body: { type: markdown, required: true }
example: |
  ## 1 - Summary

  **Objective.** Extract the PTY lifecycle into a `PtySession` so teardown is
  testable and reusable. **Done** = `AgentHost` depends on `PtySession` and
  anti-zombie teardown is covered by a test.
