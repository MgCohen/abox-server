type: context
description: What exists today and where the gap is.
rubric:
  - Describe the current state in prose, then name the specific gap this closes.
  - Cite the real files/types that exist now.
  - Enough prose to orient a reader, not just a file list.
body: markdown
