type: scope
collection: true
group: Scope
description: A bounded in/out list that draws the work boundary.
rubric:
  - One "### In scope" member (what this builds) and one "### Out of scope" (owned elsewhere, with where).
  - Bullets, each a real capability — not a vague gesture.
body: markdown
