# Rules — the sample type

A plain Rulebook (no `docType:` front matter — not a doc-engine instance). Each
`### ` header is a guarantee that a `[Rule]`-cited `[Fact]` must enforce 1:1/1:N,
kept in lockstep by the extracted ParityGuard.

### a sample guarantee holds

The consumer's first guarantee is enforced by a runnable, cited test.

### a second guarantee holds

The consumer's second guarantee is likewise enforced.
