// RemoteAgents.Flows — the Flow framework (L2: Flow base, lifecycle, snapshot
// pump) and the four flow recipes (L10: claude-only, claude-validate,
// full-review, unity-review). Intentionally empty at L1. A flow author works
// here and — by construction of the reference graph — cannot invoke a tool
// except by handing a Step to Flow.Run.
