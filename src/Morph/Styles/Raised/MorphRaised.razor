@namespace Morph
@inject MorphOptions Options

<MorphShape Class="@($"neu-raised {Class}")" Vars="@Options.Resolve(RaisedStyle.Name).Vars">@ChildContent</MorphShape>

@code {
    [Parameter] public string? Class { get; set; }

    [Parameter] public RenderFragment? ChildContent { get; set; }
}
