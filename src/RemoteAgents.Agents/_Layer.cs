// RemoteAgents.Agents — the agent abstraction and providers (L5 provider
// framework, L6 concrete Claude/Codex, L7 roles, L9 terminals, L11 hooks).
// Intentionally empty at L1. The eventual agent-driving surface lives behind an
// `internal IAgentInvocation` so only the Steps assembly can reach it.
