# Structure Rulebook — Rule template

The shape every Rule in `rules.md` must follow. Invariant header (no result arrow); exactly one `**Why:**`
bullet. Anything else a Rule needs to say is a plain prose line under it, never another bold-label bullet.

### <subject> must / must not <placement constraint>
- **Why:** <the blind spot this closes on disk>
