# Unit Rulebook — Rule template

The shape every Rule in `rules.md` must follow. Behavioral header ending in the `→` expected result; exactly
one `**Why:**` bullet. Anything else a Rule needs to say is a plain prose line under it, never another bold-label bullet.

### <subject> <condition> → <expected result>
- **Why:** <the contract this protects>
