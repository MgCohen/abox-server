# Was _schema/block.schema.yaml — now a self-describing data file.
name: block
description: A reusable content unit.
defs: blocks/*.yaml
fields:
  type:
    kind: string
    required: true
  description:
    kind: string
    required: true
  rubric:
    kind: strmap
    required: true
  body:
    kind: typespec
    required: false
  attrs:
    kind: attrs
    required: false
  collection:
    kind: bool
    required: false
  group:
    kind: string
    required: false
constraints:
  - rule: requires_when
    when: collection
    then: group
