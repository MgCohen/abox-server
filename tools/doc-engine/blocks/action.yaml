type: action
collection: true
group: Actions
composes: [step]
description: One self-contained how-to within a guide — context, ordered steps, and a stated, observable outcome.
rubric:
  outcome-stated: States a concrete end result a reader can recognize when it is reached.
  validation-observable: Validation describes a concrete, observable way to confirm the Outcome.
  steps-present: Has at least one step.
labels:
  Context:
    required: true
  Validation:
    required: true
  Outcome:
    required: true
body:
  type: markdown
