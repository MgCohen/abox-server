---
description: Walk a guide doc in a throwaway git worktree and report pass/fail per action.
---
Use the **walk-guide** subagent to walk the guide at $ARGUMENTS in throwaway git worktrees, then
report its per-action verdict (pass/fail + evidence) and whether the guide still holds.
