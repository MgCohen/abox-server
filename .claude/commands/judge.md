---
description: Run the schema-enforced judge workflow on a test file.
---

Run the judge workflow on $ARGUMENTS using `Workflow({ name: 'judge', args: '$ARGUMENTS' })`. If no argument is given, run it with no args (the workflow defaults to a sample test). Render the returned verdict for the user.
